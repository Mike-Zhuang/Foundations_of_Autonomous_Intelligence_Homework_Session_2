# Homework Session 2

## 文件结构

- `README.md`：项目说明。
- `pics/`：图片资源目录。
	- `panorama/`：全景类图片。
	- `plants/`：植物类图片。

## 图片说明

- 本次整理中，图片按内容分为两类：全景图片与植物图片，分别放在 `panorama` 和 `plants` 子目录。
- 实际操作中，若无兼容性限制，优先使用 HEIC 格式（通常在相近视觉质量下体积更优、保留信息更多）。
- 需要跨平台或网页展示时，可再转换为 JPG/PNG 等通用格式。
